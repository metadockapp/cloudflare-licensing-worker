type Bindings = {
    licenses: KVNamespace,
    activations: KVNamespace,
    hardwareIds: KVNamespace,
    tokens: KVNamespace,
    PRIVATE_KEY: string,
    PUBLIC_KEY: string,
    PASSPHRASE: string,
    API_KEY: string,
  }

  export default Bindings